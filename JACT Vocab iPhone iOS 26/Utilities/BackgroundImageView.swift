import UIKit

/// This class has no job except to identify the card background image view,
/// the image view is enabled for touches.
/// These touches will be picked up by the page controller's gesture recognizer
/// and our delegate will say "go ahead".
class BackgroundImageView: UIImageView {}
